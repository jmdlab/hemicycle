[Service]
# The hardening drop-in sets UMask=0027, which writes rendered cards 0640 —
# unreadable by nginx (www-data) and every image 404s. These cards are public
# parliamentary data with no secrecy requirement, so they must be world-readable.
UMask=0022
